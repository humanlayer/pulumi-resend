{
  "name": "resend",
  "displayName": "Resend",
  "namespace": "resend",
  "language": {
    "csharp": {
      "respectSchemaVersion": true
    },
    "go": {
      "generateResourceContainerTypes": true,
      "importBasePath": "github.com/kylemistele/pulumi-resend/sdk/go/resend",
      "respectSchemaVersion": true
    },
    "nodejs": {
      "packageName": "@pulumi/resend",
      "respectSchemaVersion": true
    },
    "python": {
      "pyproject": {
        "enabled": true
      },
      "respectSchemaVersion": true
    }
  },
  "config": {
    "variables": {
      "apiKey": {
        "type": "string",
        "description": "Resend API key. Falls back to RESEND_API_KEY when unset.",
        "secret": true
      }
    }
  },
  "types": {
    "resend:functions:Attachment": {
      "description": "An email attachment.",
      "properties": {
        "content": {
          "type": "string",
          "description": "Base64-encoded content of the attachment."
        },
        "contentId": {
          "type": "string",
          "description": "Content ID for inline attachments (e.g., for embedding images in HTML)."
        },
        "contentType": {
          "type": "string",
          "description": "The MIME type of the attachment."
        },
        "filename": {
          "type": "string",
          "description": "The filename of the attachment."
        },
        "path": {
          "type": "string",
          "description": "URL path to fetch the attachment content from."
        }
      },
      "type": "object",
      "required": [
        "filename"
      ]
    },
    "resend:functions:Tag": {
      "description": "An email tag for categorization.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The tag name."
        },
        "value": {
          "type": "string",
          "description": "The tag value."
        }
      },
      "type": "object",
      "required": [
        "name",
        "value"
      ]
    },
    "resend:resources:DnsRecord": {
      "description": "A DNS record required to verify or operate a Resend domain.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The DNS record name."
        },
        "priority": {
          "type": "integer",
          "description": "The MX priority for records that require one."
        },
        "record": {
          "type": "string",
          "description": "The Resend record purpose, such as SPF, DKIM, Receiving, Tracking, or TrackingCAA."
        },
        "status": {
          "type": "string",
          "description": "The verification status of the DNS record."
        },
        "ttl": {
          "type": "string",
          "description": "The DNS record time-to-live value."
        },
        "type": {
          "type": "string",
          "description": "The DNS record type, such as TXT, MX, CNAME, or CAA."
        },
        "value": {
          "type": "string",
          "description": "The DNS record value."
        }
      },
      "type": "object",
      "required": [
        "record",
        "name",
        "type",
        "ttl",
        "status",
        "value"
      ]
    },
    "resend:resources:TemplateVariable": {
      "description": "A Resend template variable definition.",
      "properties": {
        "fallbackValue": {
          "type": "string",
          "description": "Optional fallback value used when no value is supplied."
        },
        "key": {
          "type": "string",
          "description": "The template variable key."
        },
        "type": {
          "type": "string",
          "description": "The template variable type, such as string, number, boolean, object, or list."
        }
      },
      "type": "object",
      "required": [
        "key",
        "type"
      ]
    }
  },
  "provider": {
    "description": "The Resend provider configuration.",
    "properties": {
      "apiKey": {
        "type": "string",
        "description": "Resend API key. Falls back to RESEND_API_KEY when unset.",
        "secret": true
      }
    },
    "inputProperties": {
      "apiKey": {
        "type": "string",
        "description": "Resend API key. Falls back to RESEND_API_KEY when unset.",
        "secret": true
      }
    }
  },
  "resources": {
    "resend:index:ApiKey": {
      "description": "A Resend API key. API key changes are replaced because Resend does not support updates.",
      "properties": {
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the API key was created."
        },
        "domainId": {
          "type": "string",
          "description": "Optional domain ID that restricts this API key to a single domain."
        },
        "lastUsedAt": {
          "type": "string",
          "description": "Timestamp when the API key was last used, if any."
        },
        "name": {
          "type": "string",
          "description": "Display name for the API key."
        },
        "permission": {
          "type": "string",
          "description": "API key permission: full_access or sending_access.",
          "default": "full_access"
        },
        "token": {
          "type": "string",
          "description": "Secret API key token returned only when the key is created.",
          "secret": true
        }
      },
      "required": [
        "name",
        "token",
        "createdAt"
      ],
      "inputProperties": {
        "domainId": {
          "type": "string",
          "description": "Optional domain ID that restricts this API key to a single domain."
        },
        "name": {
          "type": "string",
          "description": "Display name for the API key."
        },
        "permission": {
          "type": "string",
          "description": "API key permission: full_access or sending_access.",
          "default": "full_access"
        }
      },
      "requiredInputs": [
        "name"
      ]
    },
    "resend:index:Domain": {
      "description": "A Resend sending domain with DNS records for verification and routing.",
      "properties": {
        "capabilities": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Enabled domain capabilities. Supported values are sending and receiving."
        },
        "clickTracking": {
          "type": "boolean",
          "description": "Whether Resend should track link clicks for this domain."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the domain was created in Resend."
        },
        "customReturnPath": {
          "type": "string",
          "description": "Optional custom return-path subdomain for bounce handling."
        },
        "name": {
          "type": "string",
          "description": "The domain name to manage in Resend."
        },
        "openTracking": {
          "type": "boolean",
          "description": "Whether Resend should track email opens for this domain."
        },
        "records": {
          "type": "array",
          "items": {
            "$ref": "#/types/resend:resources:DnsRecord"
          },
          "description": "DNS records Resend requires for domain verification and operation."
        },
        "region": {
          "type": "string",
          "description": "The Resend sending region for the domain, such as us-east-1, eu-west-1, sa-east-1, or ap-northeast-1."
        },
        "status": {
          "type": "string",
          "description": "The current Resend verification status for the domain."
        },
        "tls": {
          "type": "string",
          "description": "TLS mode for email delivery: opportunistic or enforced."
        },
        "trackingSubdomain": {
          "type": "string",
          "description": "Optional subdomain to use for click and open tracking."
        }
      },
      "required": [
        "name",
        "status",
        "records",
        "createdAt"
      ],
      "inputProperties": {
        "capabilities": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Enabled domain capabilities. Supported values are sending and receiving."
        },
        "clickTracking": {
          "type": "boolean",
          "description": "Whether Resend should track link clicks for this domain."
        },
        "customReturnPath": {
          "type": "string",
          "description": "Optional custom return-path subdomain for bounce handling."
        },
        "name": {
          "type": "string",
          "description": "The domain name to manage in Resend."
        },
        "openTracking": {
          "type": "boolean",
          "description": "Whether Resend should track email opens for this domain."
        },
        "region": {
          "type": "string",
          "description": "The Resend sending region for the domain, such as us-east-1, eu-west-1, sa-east-1, or ap-northeast-1."
        },
        "tls": {
          "type": "string",
          "description": "TLS mode for email delivery: opportunistic or enforced."
        },
        "trackingSubdomain": {
          "type": "string",
          "description": "Optional subdomain to use for click and open tracking."
        }
      },
      "requiredInputs": [
        "name"
      ]
    },
    "resend:index:DomainVerification": {
      "description": "A Resend domain verification trigger that waits for DNS verification to complete.",
      "properties": {
        "domainId": {
          "type": "string",
          "description": "The Resend domain ID to verify."
        },
        "status": {
          "type": "string",
          "description": "The current Resend verification status for the domain."
        }
      },
      "required": [
        "domainId",
        "status"
      ],
      "inputProperties": {
        "domainId": {
          "type": "string",
          "description": "The Resend domain ID to verify."
        }
      },
      "requiredInputs": [
        "domainId"
      ]
    },
    "resend:index:Template": {
      "description": "A Resend email template with HTML, optional defaults, and template variables.",
      "properties": {
        "alias": {
          "type": "string",
          "description": "Optional template alias."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the template was created."
        },
        "from": {
          "type": "string",
          "description": "Optional default sender for emails sent with the template."
        },
        "html": {
          "type": "string",
          "description": "The HTML body for the template."
        },
        "name": {
          "type": "string",
          "description": "The template name."
        },
        "replyTo": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Optional default reply-to addresses for emails sent with the template."
        },
        "subject": {
          "type": "string",
          "description": "Optional default subject for emails sent with the template."
        },
        "text": {
          "type": "string",
          "description": "Optional plain text body for the template."
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the template was last updated."
        },
        "variables": {
          "type": "array",
          "items": {
            "$ref": "#/types/resend:resources:TemplateVariable"
          },
          "description": "Optional template variable definitions."
        }
      },
      "required": [
        "name",
        "html",
        "createdAt",
        "updatedAt"
      ],
      "inputProperties": {
        "alias": {
          "type": "string",
          "description": "Optional template alias."
        },
        "from": {
          "type": "string",
          "description": "Optional default sender for emails sent with the template."
        },
        "html": {
          "type": "string",
          "description": "The HTML body for the template."
        },
        "name": {
          "type": "string",
          "description": "The template name."
        },
        "replyTo": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Optional default reply-to addresses for emails sent with the template."
        },
        "subject": {
          "type": "string",
          "description": "Optional default subject for emails sent with the template."
        },
        "text": {
          "type": "string",
          "description": "Optional plain text body for the template."
        },
        "variables": {
          "type": "array",
          "items": {
            "$ref": "#/types/resend:resources:TemplateVariable"
          },
          "description": "Optional template variable definitions."
        }
      },
      "requiredInputs": [
        "name",
        "html"
      ]
    },
    "resend:index:Topic": {
      "description": "A Resend topic for managing subscription preferences. Topics allow contacts to control which types of emails they receive.",
      "properties": {
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the topic was created."
        },
        "defaultSubscription": {
          "type": "string",
          "description": "The default subscription behavior for new contacts: opt_in (contacts must opt in) or opt_out (contacts are subscribed by default)."
        },
        "description": {
          "type": "string",
          "description": "An optional description of the topic. Maximum 200 characters."
        },
        "name": {
          "type": "string",
          "description": "The name of the topic. Maximum 50 characters."
        },
        "visibility": {
          "type": "string",
          "description": "Topic visibility: public (visible in preference center) or private (hidden from preference center)."
        }
      },
      "required": [
        "name",
        "defaultSubscription",
        "createdAt"
      ],
      "inputProperties": {
        "defaultSubscription": {
          "type": "string",
          "description": "The default subscription behavior for new contacts: opt_in (contacts must opt in) or opt_out (contacts are subscribed by default)."
        },
        "description": {
          "type": "string",
          "description": "An optional description of the topic. Maximum 200 characters."
        },
        "name": {
          "type": "string",
          "description": "The name of the topic. Maximum 50 characters."
        },
        "visibility": {
          "type": "string",
          "description": "Topic visibility: public (visible in preference center) or private (hidden from preference center)."
        }
      },
      "requiredInputs": [
        "name",
        "defaultSubscription"
      ]
    },
    "resend:index:Webhook": {
      "description": "A Resend webhook endpoint that receives event notifications.",
      "properties": {
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the webhook was created."
        },
        "endpoint": {
          "type": "string",
          "description": "The HTTPS URL that Resend will send webhook events to."
        },
        "events": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "List of event types to subscribe to. Valid events include email.sent, email.delivered, email.bounced, email.complained, email.opened, email.clicked, email.failed, email.delivery_delayed, email.scheduled, email.received, email.suppressed, domain.created, domain.updated, domain.deleted, contact.created, contact.updated, and contact.deleted."
        }
      },
      "required": [
        "endpoint",
        "events",
        "createdAt"
      ],
      "inputProperties": {
        "endpoint": {
          "type": "string",
          "description": "The HTTPS URL that Resend will send webhook events to."
        },
        "events": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "List of event types to subscribe to. Valid events include email.sent, email.delivered, email.bounced, email.complained, email.opened, email.clicked, email.failed, email.delivery_delayed, email.scheduled, email.received, email.suppressed, domain.created, domain.updated, domain.deleted, contact.created, contact.updated, and contact.deleted."
        }
      },
      "requiredInputs": [
        "endpoint",
        "events"
      ]
    }
  },
  "functions": {
    "resend:index:sendEmail": {
      "description": "Send an email using the Resend API. This is a stateless function that sends an email and returns the message ID.",
      "inputs": {
        "properties": {
          "attachments": {
            "type": "array",
            "items": {
              "$ref": "#/types/resend:functions:Attachment"
            },
            "description": "List of file attachments."
          },
          "bcc": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "List of BCC recipient email addresses."
          },
          "cc": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "List of CC recipient email addresses."
          },
          "from": {
            "type": "string",
            "description": "The sender email address. Must be a verified domain."
          },
          "headers": {
            "type": "object",
            "additionalProperties": {
              "type": "string"
            },
            "description": "Custom email headers as key-value pairs."
          },
          "html": {
            "type": "string",
            "description": "The HTML body of the email."
          },
          "replyTo": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "List of reply-to email addresses."
          },
          "scheduledAt": {
            "type": "string",
            "description": "ISO 8601 timestamp to schedule the email for future delivery."
          },
          "subject": {
            "type": "string",
            "description": "The email subject line."
          },
          "tags": {
            "type": "array",
            "items": {
              "$ref": "#/types/resend:functions:Tag"
            },
            "description": "List of tags for categorizing the email."
          },
          "template": {
            "type": "string",
            "description": "The template ID to use for the email body."
          },
          "text": {
            "type": "string",
            "description": "The plain text body of the email."
          },
          "to": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "List of recipient email addresses (maximum 50)."
          },
          "topicId": {
            "type": "string",
            "description": "Topic ID for managing unsubscribes."
          }
        },
        "type": "object",
        "required": [
          "from",
          "to",
          "subject"
        ]
      },
      "outputs": {
        "properties": {
          "emailId": {
            "description": "The unique identifier for the sent email.",
            "type": "string"
          }
        },
        "required": [
          "emailId"
        ],
        "type": "object"
      }
    }
  }
}
